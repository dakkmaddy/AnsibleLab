---
- name: fetch module demo
  hosts: pi
  become: true
  vars:
    log_file: "/var/log/pihole.log"
    dump_dir: "/srv/Splunk"
  tasks:
    - name: fetch log
      ansible.builtin.fetch:
        src: "{{ log_file }}"
        dest: "{{ dump_dir }}"
 
